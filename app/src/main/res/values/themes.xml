<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.EbookStore" parent="Theme.Material3.Light">
        <!-- Customize your light theme here. -->
         <item name="colorPrimary">@color/purple_200</item>
        <item name="colorSecondary">@color/purple_200</item>
        <item name="titleTextColor">@color/white</item>
        <item name="homeAsUpIndicator">@drawable/baseline_arrow_back_24</item>

    </style>

    <style name="Theme.EbookStore" parent="Base.Theme.EbookStore" >
        <item name="colorPrimary">@color/purple_200</item>
        <item name="colorSecondary">@color/purple_200</item>
        <item name="homeAsUpIndicator">@drawable/baseline_arrow_back_24</item>
        <item name="titleTextColor">@color/white</item>

    </style>

    <style name="Theme.login" parent="Theme.AppCompat.DayNight.NoActionBar" >
        <item name="colorPrimary">@color/purple_200</item>
        <item name="colorSecondary">@color/purple_200</item>
        <item name="homeAsUpIndicator">@drawable/baseline_arrow_back_24</item>
        <item name="titleTextColor">@color/white</item>

    </style>
    <style name="EditTextTheme" parent="Widget.AppCompat.EditText">
        <item name="android:textColorHint">#999999</item>  <!-- Hint text color -->
    </style>

    <style name="SearchBoxHintText">
        <item name="android:textColor">#999999</item>  <!-- Hint text color -->
        <item name="android:textSize">16sp</item>  <!-- Hint text size -->
    </style>

</resources>